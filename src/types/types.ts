export type SideBarType = {
  isCollapse?: boolean;
  isActive?: boolean;
};

export type ButtonType = {
  isActive?: boolean;
  isMain?: boolean;
};

export type LineType = {
  customWidth?: string;
};
